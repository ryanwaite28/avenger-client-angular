import { BypassResourceUrlPipe } from './bypass-resource-url.pipe';

describe('BypassResourceUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new BypassResourceUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
